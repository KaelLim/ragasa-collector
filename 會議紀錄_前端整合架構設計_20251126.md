# 會議紀錄：前端整合架構設計會議

**會議日期**：2025-11-26
**會議主題**：慈濟災害個人資料收集系統 - 前端整合架構設計
**專案版本**：v0.0.5（地端驗證版）
**參與團隊**：前端團隊、後端團隊、程編（Serena）、分析師（Sequential Thinking）、總協（Claude Code）

---

## 📋 會議議程

1. 專案現況分析（程編）
2. 整合架構設計（分析師）
3. 後端技術文件交付
4. 工作優先順序確認
5. 下一步行動計劃

---

## 一、專案現況分析

### 1.1 技術棧概覽

**前端框架**：
- Next.js 15.3.1 (App Router)
- React 18.3.1 (Client Components)
- TypeScript 5.6.3
- HeroUI 2.8.4 (UI 組件庫)
- Tailwind CSS 4.1.11

**後端服務**：
- Supabase Self-hosted (Docker)
- PostgreSQL + pgvector
- Supabase Auth
- Supabase Storage
- Supabase Edge Functions

### 1.2 核心業務流程（原有 UI/UX）

```
步驟 1: 慰問信 (Comfort Letter)
   └─> 閱讀並同意慰問信內容

步驟 2: 授權同意書 (Consent Form)
   └─> 閱讀並同意授權條款

步驟 3: 申請表單 (Application Form) - 4 個子步驟
   ├─> 子步驟 1: 基本資料輸入
   │   ├─ 受災者姓名 (victim_name)
   │   ├─ 身分證字號 (id_number)
   │   ├─ 聯絡電話 (phone_number)
   │   ├─ 聯絡地址 (address)
   │   ├─ 銀行代碼 (bank_code)
   │   └─ 銀行帳號 (bank_account)
   │
   ├─> 子步驟 2: 證件照片 ← **整合點 A**
   │   ├─ 身分證正面照 (CameraCapture)
   │   ├─ 身分證背面照 (CameraCapture)
   │   └─ 銀行存摺照 (CameraCapture)
   │
   ├─> 子步驟 3: 電子簽名
   │   └─ 申請人簽名 (SignatureCanvas)
   │
   └─> 子步驟 4: 確認送出 ← **整合點 B**
       └─ 顯示完整資料並送出

步驟 4: 完成 (Completed)
   └─> 顯示申請成功訊息 + 申請編號
```

### 1.3 現有服務模組

| 檔案路徑 | 功能 | 狀態 | 問題 |
|:---|:---|:---|:---|
| `heroui/services/imageAnalysisService.ts` | 圖片上傳與分析服務 | 🟡 部分完成 | UTF-8 編碼錯誤 |
| `heroui/hooks/useRealtimeImageAnalyses.ts` | 即時狀態訂閱 Hook | 🟡 部分完成 | UTF-8 編碼錯誤 |
| `heroui/hooks/useAuth.ts` | 使用者認證 Hook | ✅ 正常 | 無 |
| `heroui/lib/exifParser.ts` | EXIF 解析工具 | ✅ 正常 | 無 |
| `heroui/lib/supabase.ts` | Supabase Client | ✅ 正常 | 無 |
| `heroui/types/database.types.ts` | 資料庫型別定義 | 🟡 需更新 | 需對照後端 Schema |
| `heroui/components/CameraCapture.tsx` | 相機拍照元件 | ✅ 正常 | 需整合 EXIF 解析 |

### 1.4 關鍵檔案

**核心業務邏輯**：
- `heroui/app/application/new/page.tsx` (943 行) - 主要整合目標

**整合點識別**：
- **整合點 A**：CameraCapture 組件（子步驟 2）- 加入 EXIF 解析
- **整合點 B**：handleFinalSubmit 函數（子步驟 4）- 整合圖片上傳服務

---

## 二、整合架構設計

### 2.1 整合架構圖

```
┌────────────────────────────────────────────────────────────┐
│              【前端】原有 React App UI/UX                    │
│                                                              │
│  步驟 1: 慰問信 → 步驟 2: 授權同意書 → 步驟 3: 申請表單      │
│                                          ├─ 子步驟 1: 基本資料│
│                                          ├─ 子步驟 2: 證件照片│ ← 整合點 A
│                                          ├─ 子步驟 3: 簽名   │
│                                          └─ 子步驟 4: 確認送出│ ← 整合點 B
└────────────────────────────────────────────────────────────┘
                                 │
                                 │ 上傳圖片、EXIF 解析
                                 ↓
┌────────────────────────────────────────────────────────────┐
│            CameraCapture.tsx（整合點 A）                     │
│                                                              │
│  1. 拍照/選擇圖片                                            │
│  2. 呼叫 exifParser 解析 EXIF                                │
│  3. 傳遞 File + EXIF 到父組件                                │
└────────────────────────────────────────────────────────────┘
                                 │
                                 │ File + EXIF Data
                                 ↓
┌────────────────────────────────────────────────────────────┐
│       app/application/new/page.tsx（整合點 B）               │
│                                                              │
│  handleFinalSubmit() {                                       │
│    1. 呼叫 uploadAndProcessImage()                           │
│    2. 上傳到 Supabase Storage                                │
│    3. 寫入 image_analyses 表（status='pending'）             │
│    4. 建立 Realtime 訂閱監聽處理狀態                          │
│    5. 等待 AI 分析完成（status='completed'）                 │
│    6. 顯示分析結果                                            │
│  }                                                           │
└────────────────────────────────────────────────────────────┘
                                 │
                                 │ 上傳圖片、寫入 DB
                                 ↓
┌────────────────────────────────────────────────────────────┐
│              imageAnalysisService.ts                         │
│                                                              │
│  uploadAndProcessImage(file, exifData) {                     │
│    1. compressImage() - 壓縮圖片                             │
│    2. Storage.upload() - 上傳原圖                            │
│    3. Storage.upload() - 上傳縮圖                            │
│    4. DB.insert(image_analyses) - 寫入記錄                   │
│       - image_name, image_url, thumbnail_key                 │
│       - exif_datetime, exif_gps, exif_camera, exif_raw       │
│       - status='pending' ← 觸發 Database Trigger             │
│    5. 返回 analysisId, imageUrl, thumbnailUrl                │
│  }                                                           │
└────────────────────────────────────────────────────────────┘
                                 │
                                 │ 寫入 status='pending'
                                 ↓
┌────────────────────────────────────────────────────────────┐
│           【Supabase】Database + Storage + Realtime          │
│                                                              │
│  ┌─────────────────────────────────────────────┐            │
│  │ image_analyses 表                            │            │
│  │ - id (UUID)                                  │            │
│  │ - user_id, tenant_id                         │            │
│  │ - image_name, image_url, thumbnail_key       │            │
│  │ - exif_datetime, exif_gps, exif_camera       │ ← 前端寫入 │
│  │ - event_description                          │            │
│  │ - status='pending'                           │            │
│  │ - caption, tags                              │ ← 後端寫入 │
│  │ - caption_embedding, tags_embedding          │            │
│  │ - status='completed'                         │            │
│  └─────────────────────────────────────────────┘            │
│                     │                                        │
│                     │ Database Trigger 自動觸發              │
│                     ↓                                        │
│  ┌─────────────────────────────────────────────┐            │
│  │ processing_queue 表（處理佇列）               │            │
│  │ - image_id (關聯 image_analyses.id)          │            │
│  │ - status='pending'                           │            │
│  └─────────────────────────────────────────────┘            │
│                     │                                        │
│                     │ Edge Function 自動處理                 │
│                     ↓                                        │
│  ┌─────────────────────────────────────────────┐            │
│  │ process-image Edge Function                  │            │
│  │ 1. 讀取圖片                                   │            │
│  │ 2. 呼叫 OpenAI Vision API                    │            │
│  │ 3. 產生 caption 和 tags                      │            │
│  │ 4. 產生 embedding 向量                       │            │
│  │ 5. 更新 image_analyses                       │            │
│  │    - caption, tags                           │            │
│  │    - caption_embedding, tags_embedding       │            │
│  │    - status='completed'                      │            │
│  └─────────────────────────────────────────────┘            │
│                     │                                        │
│                     │ Realtime 推送更新                      │
│                     ↓                                        │
└────────────────────────────────────────────────────────────┘
                     │
                     │ status='completed' 事件
                     ↓
┌────────────────────────────────────────────────────────────┐
│         useRealtimeImageAnalyses Hook                        │
│                                                              │
│  useEffect(() => {                                           │
│    // 訂閱 image_analyses 表的 UPDATE 事件                   │
│    supabase                                                  │
│      .channel(`image-${analysisId}`)                         │
│      .on('postgres_changes', {                               │
│        event: 'UPDATE',                                      │
│        filter: `id=eq.${analysisId}`                         │
│      }, (payload) => {                                       │
│        if (payload.new.status === 'completed') {             │
│          // 顯示分析結果                                      │
│          setCaption(payload.new.caption)                     │
│          setTags(payload.new.tags)                           │
│        }                                                     │
│      })                                                      │
│      .subscribe()                                            │
│  }, [analysisId])                                            │
└────────────────────────────────────────────────────────────┘
```

### 2.2 資料流向

**前端 → Supabase**：
```typescript
// 前端寫入欄位
{
  user_id: string,
  tenant_id: string,
  image_name: string,
  image_url: string,
  thumbnail_key: string,
  event_description: string | null,
  exif_datetime: string | null,
  exif_gps: { lat: number, lon: number } | null,
  exif_camera: string | null,
  exif_raw: object | null,
  status: 'pending'  // 觸發 Database Trigger
}
```

**Supabase → 前端**（Realtime）：
```typescript
// 後端更新欄位（Realtime 推送）
{
  id: string,
  status: 'completed' | 'failed',
  caption: string | null,
  tags: string[] | null,
  caption_embedding: number[] | null,
  tags_embedding: number[] | null,
  error_message: string | null
}
```

### 2.3 整合策略

**方案：組件層級整合**（已選定）

**優點**：
- 保留原有 UI/UX 完整性
- 最小化程式碼改動
- 易於測試和除錯
- 保持向後相容

**整合步驟**：

1. **CameraCapture 組件整合（整合點 A）**
   ```typescript
   // 在 CameraCapture.tsx 中
   import { parseExif } from '@/lib/exifParser'

   const handleCapture = async (file: File) => {
     const exifData = await parseExif(file)
     onCapture(file, exifData)  // 傳遞 File + EXIF
   }
   ```

2. **handleFinalSubmit 函數整合（整合點 B）**
   ```typescript
   // 在 app/application/new/page.tsx 中
   import { uploadAndProcessImage } from '@/services/imageAnalysisService'
   import { useRealtimeImageAnalyses } from '@/hooks/useRealtimeImageAnalyses'

   const handleFinalSubmit = async () => {
     // 上傳身分證正面
     const frontIdResult = await uploadAndProcessImage(
       fileData.frontIdPhoto!,
       '身分證正面照'
     )

     // 訂閱 Realtime 狀態
     const unsubscribe = subscribeToProcessingStatus(
       frontIdResult.analysisId,
       (status, data) => {
         if (status === 'completed') {
           console.log('AI 分析完成:', data.caption, data.tags)
         }
       }
     )

     // 繼續原有邏輯...
   }
   ```

---

## 三、後端技術文件交付

### 3.1 文件清單

後端團隊已完成 6 份完整技術文件：

| # | 文件名稱 | 檔案路徑 | 說明 |
|:---|:---|:---|:---|
| 1 | **會議紀錄** | `會議紀錄_後端AI處理伺服器架構設計_20251126.md` | 架構設計決策、前後端職責劃分 |
| 2 | **API 規格文件** | `docs/BACKEND_API_SPECIFICATION.md` | 完整 API 端點、請求/回應格式、Supabase Client 範例 |
| 3 | **Database Schema 說明** | `docs/DATABASE_SCHEMA.md` | 5 個資料表結構、欄位型別、RLS 政策、Triggers |
| 4 | **Supabase 整合指南** | `docs/SUPABASE_INTEGRATION.md` | Client 初始化、圖片上傳流程、Storage 操作、React Hooks 封裝 |
| 5 | **Realtime 訂閱指南** | `docs/REALTIME_SUBSCRIPTION_GUIDE.md` | Realtime 基礎、Promise 封裝、React Hooks、超時處理 |
| 6 | **EXIF 解析指南** | `docs/EXIF_PARSING_GUIDE.md` | EXIF 解析實作、資料格式、GPS 轉換、UI 顯示 |

### 3.2 關鍵技術規格確認

#### ✅ Supabase 環境確認

| 項目 | 狀態 | 說明 |
|:---|:---|:---|
| Supabase 本地環境 | ✅ 運行中 | `http://localhost:8000` |
| `image_analyses` 表 | ✅ 已建立 | 含 EXIF、embedding 欄位 |
| `processing_queue` 表 | ✅ 已建立 | 處理佇列表 |
| `disaster-images` Storage | ✅ 已建立 | 圖片儲存 bucket |
| `process-image` Edge Function | ✅ 已部署 | AI 分析處理函數 |
| Database Trigger | ✅ 已建立 | 自動觸發處理佇列 |

#### 🔗 連線資訊

```bash
# API URL
http://localhost:8000

# Anon Key（從環境變數取得）
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Supabase Studio
http://localhost:8000
```

### 3.3 前後端職責劃分

#### 前端職責（寫入 image_analyses）

```typescript
interface FrontendWrite {
  // 必填欄位
  user_id: string              // 當前使用者 ID
  tenant_id: string            // 租戶 ID
  image_name: string           // 原始檔名
  image_url: string            // Storage URL
  thumbnail_key: string        // 縮圖 Storage path
  status: 'pending'            // 必須為 'pending'

  // 選填欄位
  event_description?: string   // 事件背景描述
  exif_datetime?: string       // EXIF 日期時間
  exif_gps?: { lat: number, lon: number }  // GPS 座標
  exif_camera?: string         // 相機資訊
  exif_raw?: object            // 原始 EXIF 資料
}
```

#### 後端職責（Edge Function 更新）

```typescript
interface BackendUpdate {
  // AI 分析結果
  caption: string              // 圖片標題/描述
  tags: string[]               // 關鍵字標籤

  // 向量嵌入
  caption_embedding: number[]  // 標題向量
  tags_embedding: number[]     // 標籤向量

  // 狀態更新
  status: 'completed' | 'failed'
  error_message?: string       // 錯誤訊息（如失敗）
}
```

### 3.4 整合檢查清單

前端需完成的整合項目：

- [ ] 閱讀 6 份後端技術文件
- [ ] 修復現有模組 UTF-8 編碼問題
- [ ] 對照 `DATABASE_SCHEMA.md` 更新 `types/database.types.ts`
- [ ] 對照 `SUPABASE_INTEGRATION.md` 更新 `imageAnalysisService.ts`
- [ ] 對照 `REALTIME_SUBSCRIPTION_GUIDE.md` 更新 `useRealtimeImageAnalyses.ts`
- [ ] 對照 `EXIF_PARSING_GUIDE.md` 驗證 `exifParser.ts`
- [ ] 整合 EXIF 解析到 CameraCapture 組件
- [ ] 整合圖片上傳服務到 handleFinalSubmit 函數
- [ ] 實作 Realtime 狀態顯示 UI
- [ ] 完整流程測試

---

## 四、工作優先順序確認

### 4.1 優先級定義

| 優先級 | 任務 | 預計工時 | 阻塞因素 |
|:---|:---|:---|:---|
| **P0** | 修復檔案編碼問題 | 0.5 天 | 無 |
| **P1** | 對照後端文件更新型別定義 | 0.5 天 | P0 完成 |
| **P2** | 對照後端文件更新服務模組 | 1 天 | P0 完成 |
| **P3** | 整合 EXIF 解析到 CameraCapture | 0.5 天 | P1, P2 完成 |
| **P4** | 整合圖片上傳到 handleFinalSubmit | 1 天 | P3 完成 |
| **P5** | 加入 Realtime 狀態顯示 UI | 1 天 | P4 完成 |
| **P6** | 完整流程測試 | 1 天 | P5 完成 |

**總預計工時**：5.5 天

### 4.2 詳細任務拆解

#### P0: 修復檔案編碼問題

**問題檔案**：
- `heroui/services/imageAnalysisService.ts`
- `heroui/hooks/useRealtimeImageAnalyses.ts`

**問題現象**：
```typescript
// 中文註解顯示為亂碼
// 例如：�\u0016G\n�\u0007U\u0006P�
```

**解決方案**：
1. 使用 `iconv-lite` 轉換編碼
2. 或重新輸入中文註解
3. 設定編輯器預設編碼為 UTF-8

#### P1: 對照後端文件更新型別定義

**檔案**：`heroui/types/database.types.ts`

**參考文件**：`docs/DATABASE_SCHEMA.md`

**更新內容**：
- 確認 `ImageAnalysis` 型別完整性
- 確認 `ImageAnalysisInsert` 型別（前端寫入）
- 確認 `ImageAnalysisUpdate` 型別（後端更新）
- 新增 `ProcessingQueue` 型別（如需要）

#### P2: 對照後端文件更新服務模組

**檔案**：
- `heroui/services/imageAnalysisService.ts`
- `heroui/hooks/useRealtimeImageAnalyses.ts`

**參考文件**：
- `docs/SUPABASE_INTEGRATION.md`
- `docs/REALTIME_SUBSCRIPTION_GUIDE.md`

**更新內容**：
- 確認 `uploadAndProcessImage()` 函數寫入欄位正確
- 確認 Storage bucket 名稱為 `disaster-images`
- 確認 Realtime 訂閱邏輯符合後端規格
- 加入超時處理機制（60 秒）
- 加入錯誤處理邏輯

#### P3: 整合 EXIF 解析到 CameraCapture

**檔案**：`heroui/components/CameraCapture.tsx`

**參考文件**：`docs/EXIF_PARSING_GUIDE.md`

**整合內容**：
```typescript
import { parseExif } from '@/lib/exifParser'

interface CameraCaptureProps {
  onCapture: (file: File, exifData?: ExifData) => void
  // ...
}

const handleCapture = async (file: File) => {
  const exifData = await parseExif(file)
  onCapture(file, exifData)
}
```

#### P4: 整合圖片上傳到 handleFinalSubmit

**檔案**：`heroui/app/application/new/page.tsx`

**整合內容**：
```typescript
import { uploadAndProcessImage } from '@/services/imageAnalysisService'
import { subscribeToProcessingStatus } from '@/hooks/useRealtimeImageAnalyses'

const handleFinalSubmit = async () => {
  try {
    // 上傳身分證正面
    const frontIdResult = await uploadAndProcessImage(
      fileData.frontIdPhoto!,
      '身分證正面照'
    )

    // 訂閱處理狀態
    const unsubscribe = subscribeToProcessingStatus(
      frontIdResult.analysisId,
      (status, data) => {
        if (status === 'completed') {
          console.log('身分證正面照分析完成:', data.caption)
        } else if (status === 'failed') {
          console.error('分析失敗:', data.error_message)
        }
      }
    )

    // 等待 60 秒或完成
    await waitForCompletion(frontIdResult.analysisId, 60000)

    // 繼續上傳其他照片...

  } catch (error) {
    console.error('上傳失敗:', error)
  }
}
```

#### P5: 加入 Realtime 狀態顯示 UI

**位置**：子步驟 2（證件照片）

**UI 元素**：
- 上傳進度條（0-100%）
- 處理狀態標籤
  - 上傳中...
  - 等待處理...
  - AI 分析中...
  - 分析完成 ✓
  - 分析失敗 ✗
- 分析結果預覽（caption, tags）

#### P6: 完整流程測試

**測試案例**：
1. 正常流程：拍照 → EXIF 解析 → 上傳 → AI 分析 → 結果顯示
2. 無 EXIF 流程：無 EXIF 圖片仍可正常上傳
3. AI 分析失敗：顯示錯誤訊息
4. 網路中斷：錯誤處理與重試機制
5. Realtime 超時：60 秒超時提示

---

## 五、決策與結論

### 5.1 技術決策

| 決策項目 | 決策結果 | 理由 |
|:---|:---|:---|
| 整合策略 | 組件層級整合 | 保留原有 UI/UX，最小化改動 |
| EXIF 解析時機 | CameraCapture 組件內 | 提早解析，避免後續多次讀取檔案 |
| Realtime 超時 | 60 秒 | 平衡用戶體驗與 AI 處理時間 |
| 錯誤處理策略 | 顯示錯誤但允許繼續 | 不阻塞申請流程 |
| 狀態顯示方式 | 即時更新 + 超時提示 | 提供即時反饋，避免長時間等待 |

### 5.2 風險評估

| 風險項目 | 影響等級 | 緩解措施 |
|:---|:---|:---|
| 編碼問題未解決 | 🔴 高 | P0 優先處理，阻塞其他任務 |
| 型別定義不一致 | 🟡 中 | P1 仔細對照後端 Schema |
| Realtime 連線不穩 | 🟡 中 | 加入超時與重試機制 |
| AI 分析失敗 | 🟢 低 | 允許繼續申請，事後人工審核 |
| 檔案過大上傳失敗 | 🟢 低 | 前端壓縮 + 檔案大小驗證 |

### 5.3 會議結論

1. **架構設計已確認**：採用組件層級整合策略
2. **後端文件已交付**：6 份完整技術文件
3. **技術規格已確認**：資料表、Storage、Edge Function 已就緒
4. **工作優先順序已制定**：P0-P6 共 6 個任務，預計 5.5 天
5. **風險已識別並制定緩解措施**

### 5.4 下一步行動

**立即執行**：
1. 修復 P0 編碼問題
2. 閱讀 6 份後端技術文件
3. 開始 P1 型別定義更新

**等待確認**：
- 領導審核會議結論
- 確認工作優先順序是否需調整

---

## 附錄 A：後端技術文件清單

### A.1 會議紀錄

**檔案名稱**：`會議紀錄_後端AI處理伺服器架構設計_20251126.md`

**內容概要**：
- 架構設計決策過程
- 前後端職責劃分
- 技術方案選型理由
- 實施計劃與時程

### A.2 API 規格文件

**檔案路徑**：`docs/BACKEND_API_SPECIFICATION.md`

**章節目錄**：
1. Supabase Client 初始化
2. 圖片上傳 API
3. 圖片分析查詢 API
4. 語意搜尋 API
5. Admin 控制台 API
6. 請求/回應格式範例
7. 錯誤代碼對照表

### A.3 Database Schema 說明

**檔案路徑**：`docs/DATABASE_SCHEMA.md`

**資料表清單**：
1. `image_analyses` - 圖片分析主表（20+ 欄位）
2. `processing_queue` - 處理佇列表
3. `users` - 使用者表（Auth 相關）
4. `tenants` - 租戶表（多租戶支援）
5. `bank_codes` - 銀行代碼參考表

**重要欄位說明**：
- `status`: 'pending' | 'processing' | 'completed' | 'failed'
- `exif_gps`: JSONB 格式 `{ "lat": number, "lon": number }`
- `caption_embedding`: vector(1536) - OpenAI text-embedding-3-small
- `tags_embedding`: vector(1536)

**RLS 政策**：
- 使用者只能查看自己的 image_analyses
- Admin 角色可查看所有記錄
- Edge Function 使用 Service Role Key 寫入

**Database Triggers**：
- `on_image_analysis_insert`: 自動加入 processing_queue
- `on_processing_complete`: 更新 image_analyses.status

### A.4 Supabase 整合指南

**檔案路徑**：`docs/SUPABASE_INTEGRATION.md`

**章節目錄**：
1. Supabase Client 初始化
   ```typescript
   import { createClient } from '@supabase/supabase-js'

   const supabase = createClient(
     'http://localhost:8000',
     process.env.VITE_SUPABASE_ANON_KEY!
   )
   ```

2. 完整圖片上傳流程
   ```typescript
   async function uploadImage(file: File, description: string) {
     // 1. 上傳到 Storage
     const { data: uploadData, error: uploadError } = await supabase.storage
       .from('disaster-images')
       .upload(`${userId}/${Date.now()}_${file.name}`, file)

     // 2. 取得 Public URL
     const { data: { publicUrl } } = supabase.storage
       .from('disaster-images')
       .getPublicUrl(uploadData.path)

     // 3. 寫入 image_analyses
     const { data: analysis, error: dbError } = await supabase
       .from('image_analyses')
       .insert({
         user_id: userId,
         image_name: file.name,
         image_url: publicUrl,
         event_description: description,
         status: 'pending'
       })
       .select()
       .single()

     return analysis
   }
   ```

3. Storage 操作最佳實踐
   - 檔案命名規則：`{userId}/{timestamp}_{originalName}`
   - 支援的格式：jpg, png, heic, heif
   - 檔案大小限制：50MB
   - 縮圖生成：200x200 px

4. 批次上傳範例
   ```typescript
   async function uploadMultiple(files: File[]) {
     const results = await Promise.all(
       files.map(file => uploadImage(file, '批次上傳'))
     )
     return results
   }
   ```

5. React Hooks 封裝
   ```typescript
   function useImageUpload() {
     const [uploading, setUploading] = useState(false)
     const [progress, setProgress] = useState(0)

     const upload = async (file: File) => {
       setUploading(true)
       try {
         const result = await uploadImage(file, '')
         return result
       } finally {
         setUploading(false)
       }
     }

     return { upload, uploading, progress }
   }
   ```

6. 錯誤處理最佳實踐
   - Storage 錯誤：檔案過大、格式不支援
   - Database 錯誤：欄位驗證失敗、RLS 阻擋
   - 網路錯誤：連線中斷、超時

### A.5 Realtime 訂閱指南

**檔案路徑**：`docs/REALTIME_SUBSCRIPTION_GUIDE.md`

**章節目錄**：
1. Realtime 訂閱基礎
   ```typescript
   const channel = supabase
     .channel(`image-${analysisId}`)
     .on(
       'postgres_changes',
       {
         event: 'UPDATE',
         schema: 'public',
         table: 'image_analyses',
         filter: `id=eq.${analysisId}`
       },
       (payload) => {
         console.log('Status updated:', payload.new.status)
       }
     )
     .subscribe()
   ```

2. 單一圖片狀態監控
   ```typescript
   function subscribeToImage(imageId: string, callback: Function) {
     const channel = supabase
       .channel(`image-${imageId}`)
       .on(
         'postgres_changes',
         { event: 'UPDATE', filter: `id=eq.${imageId}` },
         (payload) => callback(payload.new)
       )
       .subscribe()

     return () => supabase.removeChannel(channel)
   }
   ```

3. 多個圖片批次監控
   ```typescript
   function subscribeToUserImages(userId: string) {
     return supabase
       .channel(`user-${userId}-images`)
       .on(
         'postgres_changes',
         { event: '*', filter: `user_id=eq.${userId}` },
         (payload) => {
           if (payload.eventType === 'INSERT') {
             console.log('New image:', payload.new)
           } else if (payload.eventType === 'UPDATE') {
             console.log('Image updated:', payload.new)
           }
         }
       )
       .subscribe()
   }
   ```

4. Promise 封裝（async/await）
   ```typescript
   function waitForCompletion(
     imageId: string,
     timeout = 60000
   ): Promise<ImageAnalysis> {
     return new Promise((resolve, reject) => {
       const timer = setTimeout(() => {
         unsubscribe()
         reject(new Error('分析超時'))
       }, timeout)

       const unsubscribe = subscribeToImage(imageId, (data) => {
         if (data.status === 'completed') {
           clearTimeout(timer)
           unsubscribe()
           resolve(data)
         } else if (data.status === 'failed') {
           clearTimeout(timer)
           unsubscribe()
           reject(new Error(data.error_message))
         }
       })
     })
   }
   ```

5. React Hooks 範例
   ```typescript
   function useImageAnalysisStatus(imageId: string) {
     const [status, setStatus] = useState<string>('pending')
     const [data, setData] = useState<ImageAnalysis | null>(null)

     useEffect(() => {
       const unsubscribe = subscribeToImage(imageId, (newData) => {
         setStatus(newData.status)
         setData(newData)
       })

       return unsubscribe
     }, [imageId])

     return { status, data }
   }
   ```

6. 超時處理策略
   - 預設超時：60 秒
   - 超時後行為：提示用戶稍後查看分析記錄
   - 重試機制：允許手動重試

7. 效能優化建議
   - 只訂閱必要的欄位：`.select('id, status, caption, tags')`
   - 及時取消訂閱：使用 `removeChannel()`
   - 避免重複訂閱：檢查是否已存在 channel

### A.6 EXIF 解析指南

**檔案路徑**：`docs/EXIF_PARSING_GUIDE.md`

**章節目錄**：
1. EXIF 解析完整實作
   ```typescript
   import EXIF from 'exif-js'

   interface ExifData {
     datetime: string | null
     gps: { lat: number; lon: number } | null
     camera: string | null
     raw: any
   }

   export async function parseExif(file: File): Promise<ExifData> {
     return new Promise((resolve) => {
       EXIF.getData(file as any, function() {
         const allData = EXIF.getAllTags(this)

         resolve({
           datetime: formatDateTime(allData),
           gps: parseGPS(allData),
           camera: getCameraInfo(allData),
           raw: allData
         })
       })
     })
   }
   ```

2. 資料格式規範
   - `datetime`: ISO 8601 格式 `YYYY-MM-DDTHH:mm:ss`
   - `gps`: `{ lat: number, lon: number }` - WGS84 座標系
   - `camera`: 字串格式 `{Make} {Model}`
   - `raw`: 完整 EXIF 物件（保留所有原始資料）

3. GPS 座標轉換
   ```typescript
   function parseGPS(exifData: any): { lat: number; lon: number } | null {
     const lat = exifData.GPSLatitude
     const latRef = exifData.GPSLatitudeRef
     const lon = exifData.GPSLongitude
     const lonRef = exifData.GPSLongitudeRef

     if (!lat || !lon) return null

     return {
       lat: convertToDecimal(lat, latRef),
       lon: convertToDecimal(lon, lonRef)
     }
   }

   function convertToDecimal(dms: number[], ref: string): number {
     const decimal = dms[0] + dms[1] / 60 + dms[2] / 3600
     return (ref === 'S' || ref === 'W') ? -decimal : decimal
   }
   ```

4. 日期時間處理
   ```typescript
   function formatDateTime(exifData: any): string | null {
     const datetime = exifData.DateTime || exifData.DateTimeOriginal
     if (!datetime) return null

     // EXIF 格式: "2025:01:26 14:30:45"
     // 轉換為 ISO: "2025-01-26T14:30:45"
     return datetime.replace(/^(\d{4}):(\d{2}):(\d{2})/, '$1-$2-$3')
   }
   ```

5. UI 顯示範例
   ```typescript
   function ExifDisplay({ exifData }: { exifData: ExifData }) {
     return (
       <div>
         {exifData.datetime && (
           <p>拍攝時間: {new Date(exifData.datetime).toLocaleString()}</p>
         )}
         {exifData.gps && (
           <p>
             GPS: {exifData.gps.lat.toFixed(6)}, {exifData.gps.lon.toFixed(6)}
             <a href={`https://maps.google.com/?q=${exifData.gps.lat},${exifData.gps.lon}`}>
               查看地圖
             </a>
           </p>
         )}
         {exifData.camera && (
           <p>相機: {exifData.camera}</p>
         )}
       </div>
     )
   }
   ```

6. 錯誤處理
   - 無 EXIF 資料：返回 null 值，不阻塞上傳
   - GPS 格式異常：跳過 GPS 解析，保留其他資料
   - 日期格式異常：返回原始字串

---

## 附錄 B：團隊通訊記錄

### B.1 後端團隊訊息彙整

#### 訊息 #1（2025-11-25 19:15）
**主旨**：Supabase 前端整合文件絕對路徑

**關鍵資訊**：
- 提供 3 份文件路徑
- 本地端點：`http://localhost:8000`
- 要求前端整合 Supabase SDK

#### 訊息 #5（2025-11-25 20:28）
**主旨**：Supabase 連接技術文件與專案企劃會議紀錄

**關鍵資訊**：
- 交付完整技術文件清單
- 提供 Supabase 連線資訊
- 整合檢查清單

#### 訊息 #8（2025-11-26 08:25）
**主旨**：後端 AI 處理伺服器架構設計完成 - 前端整合文件交付

**關鍵資訊**：
- 6 份完整技術文件
- 前後端職責劃分
- 資料表職責劃分
- 重要提醒事項

---

## 附錄 C：名詞解釋

### C.1 技術術語

| 術語 | 英文 | 說明 |
|:---|:---|:---|
| EXIF | Exchangeable Image File Format | 圖片檔案的元資料格式，包含拍攝時間、GPS、相機資訊 |
| Realtime | Real-time Subscription | Supabase 提供的即時資料訂閱功能 |
| RLS | Row Level Security | PostgreSQL 的行級安全政策 |
| Embedding | Vector Embedding | 將文字轉換為數值向量，用於語意搜尋 |
| Edge Function | Serverless Function | Supabase 提供的無伺服器函數，用於後端邏輯 |
| Database Trigger | Database Trigger | 資料庫觸發器，在特定事件自動執行 |
| Storage Bucket | Object Storage | Supabase 提供的物件儲存服務 |

### C.2 狀態定義

| 狀態 | 說明 | 負責方 |
|:---|:---|:---|
| `pending` | 等待處理 | 前端寫入 |
| `processing` | 處理中 | Edge Function 更新 |
| `completed` | 處理完成 | Edge Function 更新 |
| `failed` | 處理失敗 | Edge Function 更新 |

---

## 附錄 D：參考資料

### D.1 內部文件

- 專案 CLAUDE.md：`/Users/chih-hungtseng/projects/relieffundpj-local-v0.0.5/CLAUDE.md`
- 專案架構文件：`/Users/chih-hungtseng/projects/relieffundpj-local-v0.0.5/ARCHITECTURE.md`

### D.2 後端文件（disaster_POC 專案）

- 會議紀錄：`/Users/chih-hungtseng/projects/disaster_POC/會議紀錄_後端AI處理伺服器架構設計_20251126.md`
- API 規格：`/Users/chih-hungtseng/projects/disaster_POC/docs/BACKEND_API_SPECIFICATION.md`
- Database Schema：`/Users/chih-hungtseng/projects/disaster_POC/docs/DATABASE_SCHEMA.md`
- Supabase 整合：`/Users/chih-hungtseng/projects/disaster_POC/docs/SUPABASE_INTEGRATION.md`
- Realtime 訂閱：`/Users/chih-hungtseng/projects/disaster_POC/docs/REALTIME_SUBSCRIPTION_GUIDE.md`
- EXIF 解析：`/Users/chih-hungtseng/projects/disaster_POC/docs/EXIF_PARSING_GUIDE.md`

### D.3 外部資源

- Supabase 官方文件：https://supabase.com/docs
- Next.js 官方文件：https://nextjs.org/docs
- HeroUI 官方文件：https://www.heroui.com
- exif-js 套件：https://github.com/exif-js/exif-js

---

## 會議簽署

**會議主持人**：總協（Claude Code）
**技術分析**：程編（Serena）、分析師（Sequential Thinking）
**會議日期**：2025-11-26
**會議狀態**：✅ 已完成，等待領導審核

---

**備註**：
- 本會議紀錄整合了後端團隊交付的 6 份技術文件
- 詳細技術規格請參閱附錄 A
- 工作優先順序已制定，等待領導批准後由管理師制定詳細工作計劃
